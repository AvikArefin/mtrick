[project]
name = "mtrick"
version = "0.6.0"
description = "A local, zero-config ML experiment tracker"
readme = "README.md"
authors = [
    { name = "Avik Arefin", email = "avik.me.arefin@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []
license = {text = "Apache-2.0"}
classifiers = [
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering :: Logging",
]

[project.urls]
Repository = "https://github.com/AvikArefin/mtrick"

[project.scripts]
mtrick = "mtrick.dashboard:main"

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"
